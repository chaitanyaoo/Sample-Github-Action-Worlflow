name: "hello"

on:
  push:
    branches:
      - master
      - production

jobs:
  helloworld:
    runs-on: ubuntu-latest

    steps:
      - name: "Print Hello world"
        run: echo "Hello World"
